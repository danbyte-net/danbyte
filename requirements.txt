Django>=5.2,<5.3
djangorestframework>=3.15.2,<3.17
drf-spectacular>=0.28            # OpenAPI 3 schema generation for the REST API
drf-spectacular-sidecar         # bundles Swagger UI / ReDoc assets (offline / airgapped)
django-filter>=24.3
django-cors-headers>=4.4
django-redis>=5.4
django-rq>=2.10
channels[daphne]>=4.1        # ASGI + WebSockets for real-time presence
channels-redis>=4.2          # Redis channel layer (presence group fan-out)
gunicorn>=22                 # production WSGI HTTP server (daphne handles WS only)
psycopg2-binary>=2.9.10
python-dotenv>=1.0.1
redis>=5.0.0
rq>=1.16
djangorestframework-simplejwt>=5.3.1
django-taggit>=5.1
reportlab>=4.0
nh3>=0.2                  # HTML sanitizer for rendered label templates (XSS-safe)
weasyprint>=63            # HTML+CSS -> PDF for exact-size label printing (needs
                          # Pango/cairo/gdk-pixbuf system libs - see Dockerfile)
segno>=1.6                # pure-Python QR generator for server-side label PDFs
requests>=2.32
pywinrm>=0.5          # agentless Windows DHCP/DNS sync over WinRM (NTLM/Kerberos)
openpyxl>=3.1
zensical>=0.0.45
# Monitoring / check engine
cryptography>=43.0        # secrets-at-rest (Fernet) for check credentials
lxml>=5                   # XML parsing for SAML (manylinux wheel; no system libxml2 needed)
signxml>=4                # pure-Python XML-DSig verify for SAML (rides lxml + cryptography, no xmlsec1)
acme>=4,<5                # ACME client (RFC 8555) for certificate issuance
josepy>=2                 # JOSE/JWK for the acme client (acme 4.x needs josepy 2)
dnspython>=2             # RFC2136/TSIG dynamic DNS for ACME DNS-01 auto-publish
icmplib>=3.0              # async, unprivileged ICMP for the ping checker
httpx>=0.27               # async HTTP checker
asyncssh>=2.18            # async SSH checker
pysnmp>=7.1               # async SNMP checker (v2c/v3)
pyvmomi>=8.0              # vSphere SOAP (vim25) - the only API that exposes
                          # ESXi host hardware; imported lazily, so a
                          # REST-only deployment never loads it
# Auth / MFA / directory
pyotp>=2.9                # TOTP authenticator codes for login MFA
django-auth-ldap>=5.0     # optional LDAP/AD auth (pulls python-ldap; needs
                          # OpenLDAP headers to build - only used when enabled)

# Dev / CI tooling
coverage>=7.6              # test coverage (CI + `make coverage`)
